
import { PrimaryRole, MainTask, PreferredDesign } from './types';

export const PRIMARY_ROLES = Object.values(PrimaryRole);
export const MAIN_TASKS = Object.values(MainTask);
export const PREFERRED_DESIGNS = Object.values(PreferredDesign);
